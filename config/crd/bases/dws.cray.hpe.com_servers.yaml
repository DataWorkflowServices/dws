
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.6.1
  creationTimestamp: null
  name: servers.dws.cray.hpe.com
spec:
  group: dws.cray.hpe.com
  names:
    kind: Servers
    listKind: ServersList
    plural: servers
    singular: servers
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Servers is the Schema for the servers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          data:
            items:
              description: ServersData defines the desired state of Servers
              properties:
                allocationSize:
                  description: Allocation size in bytes
                  format: int64
                  minimum: 1
                  type: integer
                hosts:
                  description: List of hosts where allocations are created
                  items:
                    description: Host specifies info required to identify the host
                      (name) on which to create storage, and the number of storage
                      allocations on each Host. ServersData.AllocationSize specifies
                      the size of each allocation.
                    properties:
                      allocationCount:
                        description: The number of allocations to create of the size
                          in bytes specified in ServerData
                        minimum: 1
                        type: integer
                      name:
                        description: The name of the host
                        type: string
                    required:
                    - allocationCount
                    - name
                    type: object
                  type: array
                label:
                  description: Label as specified in the DirectiveBreakdown
                  type: string
              required:
              - allocationSize
              - hosts
              - label
              type: object
            type: array
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
        type: object
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
